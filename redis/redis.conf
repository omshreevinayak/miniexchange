# Basic settings
port 6379
bind 0.0.0.0
protected-mode yes
requirepass redissecret

# Memory management
maxmemory 512mb
maxmemory-policy allkeys-lru

# Persistence (optional, for caching we can use RDB/AOF)
save 900 1
save 300 10
save 60 10000